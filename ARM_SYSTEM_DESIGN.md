# 机械臂部分

## 1. 主要内容

- `MainCode/`：STM32F407 主工程，包含 UDP、CAN 电机、RS485 力传感器、逆运动学、主控制器和四个模块测试工程。
- `ARM_SYSTEM_DESIGN.md`：本系统的控制内容、通信格式和行为逻辑说明。

## 2. 工程目录

```text
Challenge Cup/
├─ Reference/                    只读参考资料，不参与编译
│  ├─ Board/                     启明欣欣开发板资料
│  ├─ Chassis/                   底盘与 NUC 通信协议
│  ├─ ForceSensor/               力传感器协议
│  ├─ Motor/eRob/                零差云控电机资料
│  ├─ Motor/RobStride/            灵足时代电机资料（待补充）
│  └─ UWV_Example/               UWV 历史例程和压缩包
├─ MainCode/                     STM32F407 主工程
│  ├─ App/                       应用协议、驱动封装和控制逻辑
│  ├─ Core/                      板级初始化和主循环
│  ├─ Config/                    CubeMX IOC 配置
│  ├─ Drivers/                   STM32 HAL、CMSIS 和 PHY 驱动
│  ├─ Middlewares/               lwIP
│  ├─ Projects/ModuleTests/      四个独立模块测试工程
│  ├─ Tests/                     MinGW 主机单元测试
│  ├─ docs/                      协议、硬件和实现说明
│  ├─ TestGuides/                上板测试步骤和验收标准
│  ├─ tools/                     构建、烧录和 UDP 调试工具
│  └─ output/                    已生成的 ELF/HEX/BIN/MAP 产物
└─ ARM_SYSTEM_DESIGN.md          交付用系统设计说明
```

`MainCode/build/` 是 CMake 的临时构建缓存，可以随时重新生成；`MainCode/output/` 是可直接烧录或归档的构建产物。两者都不属于源代码。

## 3. 系统作用

STM32 负责六个电机的实时控制、力传感器采集和状态回传；NUC 负责视觉、路径规划和任务流程，并通过 UDP 下发机械臂目标位置。

系统通路如下：

```text
NUC UDP 指令
    -> ASCII 指令解析
    -> 逆运动学
    -> J1~J5 位置控制
    -> J6 夹爪控制
    -> RS485 力传感器确认抓取
    -> UDP 状态回传
```

## 4. 硬件与电机分配

| 关节 | 功能 | 驱动器 | 总线 | CAN ID | 控制量 |
| --- | --- | --- | --- | ---: | --- |
| J1 | 底部 Yaw | eRob | CAN1 | 11 | 编码器计数 |
| J2 | Pitch | eRob | CAN1 | 12 | 编码器计数 |
| J3 | Pitch | eRob | CAN1 | 13 | 编码器计数 |
| J4 | Pitch | eRob | CAN1 | 14 | 编码器计数 |
| J5 | 顶部 Pitch | RobStride | CAN2 | 15 | rad |
| J6 | 夹爪开合 | RobStride | CAN2 | 16 | rad |

所有电机初始按编码器中位处理，六个电机采用同一旋向。统一方向参数为：

```c
#define APP_ARM_DIRECTION 1.0F
```

以太网使用 RMII，UDP 默认端口为 `20001`。力传感器使用 `USART6` 外接 3.3 V 半双工 RS485 收发器：PC6/PC7 为数据线，PB0 为 DE 与 `/RE` 控制。UDP 和 RS485 使用不同外设、引脚和 DMA 资源，可以同时工作。

## 5. 机械臂模型与逆运动学

坐标系为右手系：`x` 向车体前方，`y` 向车体左方，`z` 向上。原点为底盘中心的机械臂基座平面，基座离地高度为 `254.25 mm`，因此输入坐标中的 `z=0` 就是基座平面。

```text
l1 = 419.90 mm
l2 = 122.95 mm
l3 = 124.00 mm
l4 = 208.12 mm
```

J2~J5 在同一平面内运动，末端连杆 `l4` 始终保持水平：

```text
q2 + q3 + q4 + q5 = 0
```

逆解过程：

1. 使用 `q1 = atan2(y, x)` 计算底部 Yaw。
2. 将目标转换为平面位置，使用阻尼最小二乘法求 J2~J5。
3. 优先保持上一时刻姿态，并在关节范围内选择运动量较小的解。
4. 目标不可达或超出限位时，不发送不完整的电机命令。

## 6. NUC 指令

### 5.1 末端位置

```text
PXYZ:+030000,-001250,+012500,/
```

三个坐标单位为 `0.01 mm`，上例表示车体坐标 `(300.00, -12.50, 125.00) mm`。固件收到位置指令后完成逆解，并向 J1~J5 下发目标位置。

### 5.2 夹爪

```text
JZH_+500_1000/
JZH_-500_1000/
```

- 第一个字段为 `0.001 rad`：正值闭合，负值打开。
- 第二个字段为最大电流，单位 mA。

闭合过程中，力传感器的合力达到 `5 N` 阈值后，固件停止 J6 并将状态置为 `GRIPPED`。

### 5.3 组合与停止

位置和夹爪可以放在同一报文中：

```text
PXYZ:+030000,+000000,+012500,SQ_1000,JZH_+500_1000,/
```

固件先执行位置动作，等待机械臂稳定后再执行夹爪动作；首次收到指令时若电机仍在使能稳定期，目标会保留并由主循环自动重试。`ST:P/` 停止当前六个电机并将状态置为 `STOPPED`；命令超时或通信掉线时执行相同的停止路径并清除待执行目标。

## 7. 主控制行为

```text
收到 PXYZ
  -> 检查运动许可和电机反馈
  -> 逆运动学求解
  -> 使能并等待 J1~J5 就绪
  -> 下发五个关节位置
  -> 状态 MOVING

收到 JZH 闭合
  -> 设置 J6 电流上限
  -> 下发 J6 位置
  -> 力达到阈值：停止 J6，状态 GRIPPED

收到 JZH 打开
  -> 下发 J6 打开位置
  -> 状态 GRIPPER_OPENING

收到 ST:P 或命令超时
  -> 停止已跟踪的六个电机
  -> 状态 STOPPED
```

主控制器只在逆解成功、驱动器已使能且反馈未超时时发送位置命令。任何故障、不可达目标或反馈失效都会进入停止/错误路径。

## 8. 状态回传

固件周期发送 `UDP_MSG_ARM_STATUS (0x83)`，回传：

- 当前状态：`IDLE`、`MOVING`、`GRIPPED`、`STOPPED`、`ERROR` 等；
- 目标坐标和 J1~J5 目标角度；
- J6 目标位置、电流限制和力传感器合力；
- 逆解位置误差、姿态误差和最近一次指令时间。

NUC 根据 `ARM_STATUS` 和力反馈决定下一步移动、抓取、返回或放置动作。
