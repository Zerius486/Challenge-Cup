# 夹取框架测试

烧录主工程，先单独完成 UDP、RS485 和两类电机测试。NUC 发送 `PXYZ:x,y,z,/`，其中坐标单位为 0.01 mm；主控在板上计算 J1~J5 逆运动学并移动到目标。稳定后发送 `JZH_+500_1000/` 闭合或 `JZH_-500_1000/` 打开。

通过标准：可达目标能使 J1~J5 到位，l4 保持水平；不可达目标返回错误且电机不执行不完整命令；闭合时力传感器合力达到 5 N 后 J6 停止并回报 `GRIPPED`，打开时回报 `GRIPPER_OPENING`。发送 `ST:P/` 或停止 UDP 后六个关节进入停止路径。
