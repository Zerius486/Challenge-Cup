# VOFA+ FireWater 测试

主工程烧录后，用 3.3 V USB-TTL 连接 PB10（开发板 TX）到转换器 RX，并共地。串口参数 460800、8N1。VOFA+ 选择 FireWater 协议，打开对应 COM 口。

通过标准：约每 100 ms 收到一行完整 CSV，行尾为 CRLF，固定 39 列；时间和帧计数递增，力传感器/电机/机械臂状态变化能在对应列反映，异常时错误标志置位。VOFA 回传仅用于观察，不应接入控制回路。
